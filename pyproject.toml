[project]
name = "Charlie-Brown-Detector"
version = "0.1.0"
description = "A YOLO trainer model that can detect Charlie Brown in different media."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ultralytics>=8.4.33",
]
